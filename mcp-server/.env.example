# OpenL Tablets MCP Server Configuration

# OpenL Tablets WebStudio Base URL (required)
# Default: http://localhost:8080/webstudio/rest
OPENL_BASE_URL=http://localhost:8080/webstudio/rest

# ============================================================================
# Authentication Configuration (choose ONE method)
# ============================================================================

# Authentication Method 1: Username and Password (Basic Auth)
# Use this for basic authentication
OPENL_USERNAME=admin
OPENL_PASSWORD=admin

# Authentication Method 2: API Key
# If using API key, comment out username/password above
# OPENL_API_KEY=your-api-key-here

# Authentication Method 3: OAuth 2.1
# If using OAuth 2.1, comment out username/password and API key above
# OPENL_OAUTH2_CLIENT_ID=your-client-id
# OPENL_OAUTH2_CLIENT_SECRET=your-client-secret
# OPENL_OAUTH2_TOKEN_URL=https://auth.example.com/oauth/token
# OPENL_OAUTH2_AUTHORIZATION_URL=https://auth.example.com/oauth/authorize
# OPENL_OAUTH2_SCOPE=openl:read openl:write
# OPENL_OAUTH2_GRANT_TYPE=client_credentials
# OPENL_OAUTH2_REFRESH_TOKEN=your-refresh-token-if-available

# ============================================================================
# Additional Configuration
# ============================================================================

# Client Document ID (optional)
# Used for request tracking and correlation
# OPENL_CLIENT_DOCUMENT_ID=mcp-server-instance-1

# Request Timeout (optional, in milliseconds)
# Default: 30000 (30 seconds)
# OPENL_TIMEOUT=30000

# ============================================================================
# Notes
# ============================================================================
# - Only one authentication method is needed:
#   * Basic Auth (username/password)
#   * API Key
#   * OAuth 2.1 (client_credentials or refresh_token)
# - For production, always use HTTPS URLs
# - Never commit this file with real credentials
# - Copy this file to .env and update with your actual values
# - Client Document ID helps track requests across systems
