# Removed Repository Workspace Outcomes

These scenarios verify the final workspace state when a design repository is removed from the OpenL Studio
configuration:

- All opened and in-editing copies linked to the repository are removed.
- Removed copies are not converted into local projects.
- Restoring the repository lists its source projects as closed without restoring the removed copies or
  local changes.
