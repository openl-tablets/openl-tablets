package org.openl.rules.rest.settings.model;

public enum UserManagementMode {

    // User management is enabled. Users are stored in the application.
    INTERNAL,
    // User management is enabled. Users are stored in an external system.
    EXTERNAL

}
