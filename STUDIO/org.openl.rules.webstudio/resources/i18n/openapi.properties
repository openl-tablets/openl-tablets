# OpenAPI specification allows the formatting of description fields at all levels.
# Thus, according to the specification, wherever the description field is permissible, we can format it,
# and the description field conforms to the CommonMark formatting style
# @see https://spec.commonmark.org/0.30
repos.create-project-from-zip.desc = The main goal is to create a new OpenL Project or update from a given template (template will be provided in the request body) and store it in the requested Design Repository. The repository name can be taken from the previous API. New project is always created in the main branch if the requested repository is Git.
repos.create-project-from-zip.summary = Single Project API by Design Repository
repos.create-project-from-zip.param.project-name.desc = Final project name. This parameter must be unique and will be used for project folder name and in rules.xml
repos.create-project-from-zip.param.path.desc = Project path in target repository. This parameter is optional and can be provided only if Flat folder structure isn't enabled for target repository.
repos.create-project-from-zip.param.comment.desc = Optional comment used as initial commit Git message. If it's empty, the default commit message will be used like: \n```\nThe project <project-name> is created\n```
repos.create-project-from-zip.param.overwrite.desc = Optional parameter used to specify if requested resource can be overwritten or not. \n\
  If a resource doesn't already exist, an overwrite flag is ignored.\n\
  If resource exists:\n\
  * If overwrite = `true`, resource must be updated.\n\
  * If overwrite = `false`, returns 409 Conflict error
repos.create-project-from-zip.param.template.desc = Project Template Binary. The file type can be provided in the header field `Content-Type`:\n\
   * ZIP (`application/zip`)

repos.get-project-list-by-repository.desc = By default, the project list will be returned based on master branch
repos.get-project-list-by-repository.summary = Get Project List by Repository
repos.get-project-list-by-repository.200.desc = The Project List or Empty List by Repository

repos.get-repository-list.desc = Retrieve the List of configured Design Repositories
repos.get-repository-list.summary = Get Design Repositories
repos.get-repository-list.200.desc = The list of configured Design Repositories

tags.get-types.desc = Finds all types
tags.get-types.summary = Get all types

tags.add-tag-type.summary = Create new tag type
tags.add-tag-type.desc = Creates new tag type

tags.delete-tag-type.summary = Delete tag type
tags.delete-tag-type.desc = Deletes tag type by ID

tags.update-tag-type.summary = Update existing tag type
tags.update-tag-type.desc = Updates existing tag type by ID

tags.delete-tag.summary = Delete existing tag from tag type
tags.delete-tag.desc = Deletes tag from tag type by ID

tags.add-tag.summary = Add new tag to tag type
tags.add-tag.desc = Adds new tag to existing tag type

tags.update-tag.summary = Update existing tag in tag type
tags.update-tag.desc = Updates existing tag in tag type

tags.tag-type.id.desc = Tag type ID
tags.tag.id.desc = Tag ID

info.get-sys-info.summary = Get System Information
info.get-sys-info.desc = Returns System Information like Java version, OS version, current type, available used resources RAM, CPU, threads, and etc.

info.get-openl-info.summary = Get OpenL Information
info.get-openl-info.desc = Returns OpenL Information like version, build number, build version, and etc.

info.get-build-info.summary = Get custom build information.
info.get-build-info.desc = Gets information from `build-info.properties` file, if exists in application resources

notif.get-notif.summary = Get notification message
notif.get-notif.desc = Returns notification message if exists, or empty
notif.get-notif.200.desc = Current Text Notification or empty

notif.post-notif.summary = Save new notification message
notif.post-notif.desc = Saves new text notification. If request body is empty, removes existing notification
notif.post-notif.req-body.desc = Notification text to save or empty to remove existing

module.is-module-modified.summary = Check module modification
module.is-module-modified.desc = Checks if current project module is modified.\n\
  Returns the following:\n\
  * `true` if modified\n\
  * `false` if not modified\n\
  * Empty response if it is not possible to get module status
module.is-module-modified.200.desc = Modification status

diff.compare-xls.summary = Compare Excel files
diff.compare-xls.desc = Compares two Excel files
diff.compare-xls.303.desc = Comparison Result Location
diff.field.file.1 = First Excel File
diff.field.file.2 = Second Excel File
diff.field.file-name = File Name

trace.get-nodes.summary = Get trace nodes
trace.get-nodes.desc = Gets child trace nodes by parent node ID
trace.field.showRealNumbers = Number pretty format. If `true`, `0.30000000000001` will be displayed as `0.3`
trace.field.id = Parent trace node ID

conflict.merged.summary = Get merged changes
conflict.merged.desc = Gets merged changes
conflict.merged.200.desc = Merged Excel file

conflict.local.summary = Get local changes
conflict.local.desc = Gets local changes
conflict.local.200.desc = Local Excel file

conflict.repository.summary = Get changes from repository
conflict.repository.desc = Get changes from repository
conflict.repository.200.desc = Excel file from repository

conflict.field.repo-id = Design Repository ID
conflict.field.name = Artifact Name
conflict.field.version = Artifact Version

header.content-disposition.desc = This header indicating if the content is expected to be displayed inline in the browser, that is, as a Web page or as part of a Web page, or as an attachment, that is downloaded and saved locally.
header.set-cookie.desc = Contains the session cookie which name is started with `response-monitor_` plus `response-monitor` value from query request parameter.

compile.get-compile.summary = Get project compilation progress
compile.get-compile.desc = Gets project compilation progress
compile.get-compile.field.messageId.desc = Latest message ID
compile.get-compile.field.messageIndex.desc = Latest message index

compile.get-compile-test.summary = Get test table compilation readiness
compile.get-compile-test.desc = Get test table compilation readiness
compile.get-compile-test.field.tableId.desc = Test table ID

compile.tests.summary = Get tests
compile.tests.desc = Get tests

compile.project.summary = Get project compilation readiness
compile.project.desc = Get project compilation readiness

compile.table.summary = Get table compilation progress
compile.table.desc = Get table compilation progress
compile.table.field.tableId.desc = Table ID

test.download.summary = Download test results
test.download.desc = Download test results
test.field.test-table-id = Test table ID
test.field.test-range = Comma separated test case IDs
test.field.per-page = Tests per page

test.manual.summary = Manual
test.manual.desc = Manual

mgmt.get-groups.desc = Get all groups with their privileges and subgroups
mgmt.get-groups.summary = Get all groups

mgmt.delete-group.desc = Delete group by given name
mgmt.delete-group.summary = Delete group

mgmt.schema.group.name = Group name
mgmt.schema.group.old-name = Previous group name
mgmt.schema.group.description = Group description
mgmt.schema.group.sub-groups = Subgroups
mgmt.schema.group.privileges = Group Privileges

mgmt.save-group.desc = Save new group or update existed one
mgmt.save-group.summary = Save or update group

mgmt.save-settings.desc = Save groups settings
mgmt.save-settings.summary = Save groups settings

mgmt.get-settings.desc = Get groups settings
mgmt.get-settings.summary = Get groups settings

mgmt.get-privileges.desc = Get privileges
mgmt.get-privileges.summary = Get privileges

mgmt.search-external-groups.desc = Search external groups
mgmt.search-external-groups.summary = Search external groups
mgmt.search-external-groups.param.search = Search term e.g. full group name or fragment
mgmt.search-external-groups.param.page-size = Number of maximum results

history.delete-all-history.summary = Clear history
history.delete-all-history.desc = Clear history

history.restore.summary = Restore from history
history.restore.desc = Restore from history
history.restore.req-body.desc = History ID

history.get-project-history.summary = Get project history
history.get-project-history.desc = Get currently opened project history

mail.verify.desc = Verify e-mail
mail.verify.summary = Verify e-mail
mail.verify.param.token = Verification token

mail.send-verification.summary = Send verification token
mail.send-verification.desc = Send verification token
mail.send-verification.param.username = Username

mail.mail-config.summary = Mail settings
mail.mail-config.desc = Mail settings

mail.update-mail-config.summary = Update mail configuration
mail.update-mail-config.desc = Update mail configuration

msg.get-stacktrace.summary = Get message stacktrace
msg.get-stacktrace.desc = Get message stacktrace
msg.param.message-id = Message ID

msg.get-url.summary = Get message URL
msg.get-url.desc = Get message URL

users.field.username = Username

users.get-users.summary = Get all users
users.get-users.desc = Get all users

users.get-user.summary = Get user details
users.get-user.desc = Get user details by username

users.add-user.summary = Create new user
users.add-user.desc = Create new user

users.edit-user.summary = Edit user
users.edit-user.desc = Edit existing user

users.edit-user-info.summary = Edit current user info
users.edit-user-info.desc = Edit current user info

users.edit-user-profile.summary = Edit current user profile
users.edit-user-profile.desc = Edit current user profile

users.get-user-profile.summary = Get current user profile
users.get-user-profile.desc = Get current user profile

users.delete-user.summary = Delete user
users.delete-user.desc = Delete existing user

users.options.summary = User options
users.options.desc = User options

users.get-user-external-groups.summary = Get user external groups
users.get-user-external-groups.desc = Get user external groups

users.get-user-external-groups.param.matched.username = * If not defined, returns all external groups for user \n\
  * If matched = `true`, returns all external groups which are matched with WebStudio groups\n\
  * If matched = `false`, returns all external groups which are not matched with WebStudio groups

repo.param.project-name.desc = Project name
repo.param.project-version.desc = Project version

repo.get-projects.summary = Get all projects
repo.get-projects.desc = Gets all projects from default design repository

repo.get-last-project.summary = Download project
repo.get-last-project.desc = Download project

repo.get-project.summary = Download project by specific revision
repo.get-project.desc = Download project by specific revision

repo.add-project.summary = #2 Create or update project from zip file
repo.add-project.desc = Uploads a zipped project to a design repository. The upload will be performed if the project in the design repository is not locked by other user.

repo.add-project.1.summary = Create or update project from zip file
repo.add-project.1.desc = Uploads a zipped project to a design repository. The upload will be performed if the project in the design repository is not locked by other user.

repo.lock-project.summary = Lock project
repo.lock-project.desc = Locks the given project. The lock will be set if this project is not locked.

repo.unlock-project.summary = Unlock project
repo.unlock-project.desc = Unlocks the given project. The unlock will be set if this project is locked by current user.
