package org.openl.rules.repository.api;

/**
 * Repository where each project is mapped to its own Folder.
 * It means that each external folder has it's own internal folder.
 * This repository can manage this mapping.
 */
public interface MappedFolderRepository extends FolderRepository {
}
