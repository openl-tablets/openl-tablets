<project>
    <name>project3</name>

    <!-- OpenL project includes one or more modules.  -->
    <modules>
        <module>
            <rules-root path="*.xlsx"/>
        </module>
    </modules>

    <dependencies>
        <dependency>
            <name>domain</name>
            <autoIncluded>false</autoIncluded>
        </dependency>
        <dependency>
            <name>project1</name>
            <autoIncluded>false</autoIncluded>
        </dependency>
    </dependencies>

    <!-- Project's classpath. -->
    <classpath>
        <entry path="."/>
    </classpath>
</project>