<project>
    <name>project3</name>

    <!-- OpenL project includes one or more modules.  -->
    <modules>
        <module>
            <rules-root path="*.xlsx"/>
        </module>
    </modules>

    <dependencies>
        <dependency>
            <name>project2</name>
            <autoIncluded>false</autoIncluded>
        </dependency>
    </dependencies>

    <!-- Project's classpath. -->
    <classpath>
        <entry path="."/>
    </classpath>
</project>