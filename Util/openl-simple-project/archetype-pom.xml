<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.openl.example</groupId>
    <artifactId>openl-simple-project</artifactId>
    <packaging>pom</packaging>
    <version>5.15.3-SNAPSHOT</version>
    <name>OpenL Simple Project Archetype POM</name>
    <properties>
        <openlVersion>${project.version}</openlVersion>
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                    <resources>
                        <resource>
                            <directory>${project.basedir}</directory>
                            <includes>
                                <include>archetype.properties</include>
                            </includes>
                            <filtering>true</filtering>
                        </resource>
                    </resources>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin> <!-- don't install this pom -->
                <artifactId>maven-install-plugin</artifactId>
                <version>2.5.2</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin> <!-- Build pom.xml -->
                <artifactId>maven-invoker-plugin</artifactId>
                <version>2.0.0</version>
                <configuration>
                    <pom>pom.xml</pom>
                    <skipInstallation>true</skipInstallation>
                    <streamLogs>true</streamLogs> <!-- the build logs should be output to the normal mojo log. -->
                    <noLog>true</noLog> <!-- Suppress logging to the build.log file. -->
                    <properties>
                        <openlVersion>${project.version}</openlVersion>
                    </properties>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase> <!-- bind to the packaging phase -->
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin> <!-- Make an archetype from this project -->
                <artifactId>maven-archetype-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <archetypePostPhase>install</archetypePostPhase>
                    <propertyFile>${project.build.directory}/archetype.properties</propertyFile>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase> <!-- bind to the packaging phase -->
                        <goals>
                            <goal>create-from-project</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
