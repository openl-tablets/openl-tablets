# Apply the migration directly (dryRun=false) so verify.groovy can assert the deleted poms.
# Aggregator goal — runs once on the reactor root (the anchor).
invoker.goals = openl:pomless -Dopenl.pomless.dryRun=false
