# The OpenL project is nested under the 'rating' submodule. openl:pomless collapses the
# pass-through 'rating' aggregator and anchors at the top with <flattenGroupId>true</>.
# Aggregator goal — applied (dryRun=false) so verify.groovy can assert the edited poms.
invoker.goals = openl:pomless -Dopenl.pomless.dryRun=false
