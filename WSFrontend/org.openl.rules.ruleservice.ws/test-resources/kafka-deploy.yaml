consumer.configs:
  bootstrap.servers: localhost:9092
  group.id: openl-ruleservice
  max.poll.records: 10

producer.configs:
  bootstrap.servers: localhost:9092
  acks: all

dlt.producer.configs:
  bootstrap.servers: localhost:9092
  acks: all

methods:
  - method.name: method1
